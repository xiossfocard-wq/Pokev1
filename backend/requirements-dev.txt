# Dependances utiles seulement pour LANCER LES TESTS, pas pour faire tourner
# l'application. A installer en plus de requirements.txt :
#
#   pip install -r requirements.txt -r requirements-dev.txt --break-system-packages
#
# httpx est ce dont `fastapi.testclient.TestClient` a besoin pour appeler
# l'API sans ouvrir de vrai port reseau (voir tests/test_api_routes.py).
# Sans lui, ce module de tests est saute proprement, le reste de la suite
# tourne normalement.
httpx>=0.27,<1.0
