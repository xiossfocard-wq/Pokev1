# Copier en .env.local pour le dev local, et définir la même variable dans
# les réglages de projet Vercel pour la prod (Settings -> Environment Variables).

# URL du backend déployé (Render, ou localhost en dev)
NEXT_PUBLIC_API_URL=http://localhost:8000
